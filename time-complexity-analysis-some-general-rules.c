#include <stdio.h>

// Big O Notation

// calculating the running time of algorithm

function()
{
	int a;  //
	a = 5;  //                                O(1)
	a++;    //
	
		// for(i = 0; i < n; i++)      
		// {
			// simple statements               O(n)
		// }
		
		// for(i = 0; i < n; i ++)
		// {
		// 		for(j = 0; j < n; j++)
		//		{
					// simple statements       O(n^2)
		//		}
		//	}
		
		// Running time of this function 'll be the sum of all the running time of these fragments
		// Running time -> T(n) = O(1) + O(n) + O(n^2) = O(n^2)
}
int main(int argc, char** argv)
{
	
	return 0;
}